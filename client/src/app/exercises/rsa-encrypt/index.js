import Component from "./RsaEncrypt";
import store from "./rsa-encrypt.store";

export default {
    // Este nombre es el que indica como se accede desde storage. En este caso:
    //    storage.rsaEncrypt
    name: "rsaEncrypt", 
    Component,
    store
};
